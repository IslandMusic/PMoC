<<<<<<< Original<<<<<<< Original<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="islandmusic.apeoplesmapofcorvallisapp">

<!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<!--android:name="android.permission.INTERNET"-->

<uses-sdk android:minSdkVersion="14" android:targetSdkVersion="23" />

<application android:debuggable="true" android:allowBackup="true" android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name" android:supportsRtl="true" android:theme="@style/AppTheme">
    android:name="android.support.multidex.MultiDexApplication"

    <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
    <meta-data android:name="com.google.android.geo.API_KEY" android:value="@string/google_maps_key" />


    <!--<activity
            android:name=".Welcome"
            android:label="@string/title_activity_welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->

    <!--Multiple intent filters for
https://developer.android.com/guide/components/intents-filters.html
https://developer.android.com/guide/components/services.html

<service android:name=".ExampleService" />
-->
    <!-- ** X ** -->
    <!-- ** Main Launcher, asks for a name and then a button click ** -->
    <activity android:name=".WelcomeScreen" android:label="@string/title_welcome">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />


        </intent-filter>
    </activity>

    <!-- ** X ** -->
    <!-- ** Accepts name and sends to main activity ** -->
    <activity android:name="islandmusic.apeoplesmapofcorvallisapp.StartApp">


    </activity>

    <!-- ** X ** -->
    <!-- ** Gets called to display Gmap ** -->
    <activity android:name=".MapSelector" android:label="@string/title_activity_map_selector">
        <intent-filter>
            <!--<action android:name="android.intent.action.MAIN" />
                 <category android:name="android.intent.category.LAUNCHER" />-->


        </intent-filter>
    </activity>

    <!-- ** X ** -->
    <!-- ** Main Screen Called after App Start ** -->
    <activity android:name="islandmusic.apeoplesmapofcorvallisapp.MainScreenActivity">

    </activity>


</application>
</manifest>

